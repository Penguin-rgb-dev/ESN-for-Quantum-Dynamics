name: quspin_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - matplotlib-base
  - joblib
  - quspin
  - libgomp  # Necessary for OpenMP parallelization on Linux/HPC
  - ipykernel # So you can use it in Jupyter/VS Code
